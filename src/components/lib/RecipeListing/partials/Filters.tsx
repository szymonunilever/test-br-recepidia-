import React from 'react';
import { RecipeFilterProps } from './models';

const Filter = ({
  onChangeSorting,
  onChangeFilter,
  onTagRemove,
}: RecipeFilterProps) => {
  // TODO: develop Filter
};

export default Filter;
