import React from 'react';
import { GlobalNavigationProps } from './models';

const GlobalNavigation = ({ list }: GlobalNavigationProps) => {
  return <div>global nav</div>;
};

export default GlobalNavigation;
