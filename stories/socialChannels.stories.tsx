import { storiesOf } from '@storybook/react';
import React from 'react';
import SocialChannels from 'src/components/lib/SocialChannels';

storiesOf('Components/SocialChannels', module).add('default', () => (
  <SocialChannels />
));
